import {Stack} from 'expo-router';

export default function ProviderOnboardingLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}